# Artifact 1 has no C/C++ sources to compile; the search is pure Python.
# Provided for parity with artifact2_e2e/Makefile and the top-level build flow.

.PHONY: all clean

all:
	@echo "[artifact1_nas] No native sources to build. Use: bash run.sh"

clean:
	@rm -rf results/*/checkpoint results/*/logs figures
	@echo "[artifact1_nas] cleaned"
